using System.Collections;
using System.Collections.Generic;
using Ring;
using UnityEngine;

public class ListZombieController : RingSingleton<ListZombieController>
{
    public List<ZombieHealth> _listZombies;

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}
